# Base URL of the backend API.
# Leave EMPTY for local development — the Vite dev server proxies /api to
# http://localhost:4000 automatically.
# In production (Vercel), set this to your Render backend URL, e.g.:
#   VITE_API_BASE_URL=https://whc-api.onrender.com
VITE_API_BASE_URL=
